import React from "react";
import ReactDOM from "react-dom/client";
import ResearchAgent from "./ResearchAgent.jsx";

ReactDOM.createRoot(document.getElementById("research-agent-root")).render(
  <React.StrictMode>
    <ResearchAgent />
  </React.StrictMode>
);
